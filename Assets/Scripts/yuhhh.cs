using UnityEngine;

public class yuhhh : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        ball(); //This is a custom function
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void ball()
    {
        Debug.Log("Hello World"); //This is the print for the debug log of Hello World
    }
}
